package com.ds.goroute.type;

/** Section a property amenity is listed under on the stay detail page. Declaration order is display order. */
public enum HotelAmenityGroup {
    GENERAL,
    FOOD_DRINK,
    WELLNESS,
    SERVICES,
    TRANSPORT,
    FAMILY,
    BUSINESS,
    SAFETY,
    /** Free text a partner typed that is not a known code. */
    OTHER
}
