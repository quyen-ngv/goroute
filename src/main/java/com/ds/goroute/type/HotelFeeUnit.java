package com.ds.goroute.type;

/** What a property fee is charged per. */
public enum HotelFeeUnit {
    PER_STAY,
    PER_NIGHT,
    PER_PERSON,
    PER_PERSON_PER_NIGHT,
    PER_ROOM_PER_NIGHT,
    PER_ITEM
}
