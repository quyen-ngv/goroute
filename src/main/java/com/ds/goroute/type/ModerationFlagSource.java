package com.ds.goroute.type;

/** The three sources that feed the single human queue (MOD-06). */
public enum ModerationFlagSource {
    TEXT_FILTER,
    AI_TEXT_FILTER,
    IMAGE_MODERATION,
    USER_REPORT
}
