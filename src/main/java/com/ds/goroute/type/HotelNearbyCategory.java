package com.ds.goroute.type;

/** Kind of a nearby place an operator lists on a stay, used for its icon. */
public enum HotelNearbyCategory {
    TRANSIT,
    AIRPORT,
    ATTRACTION,
    SHOPPING,
    RESTAURANT,
    BEACH,
    HOSPITAL,
    OTHER
}
