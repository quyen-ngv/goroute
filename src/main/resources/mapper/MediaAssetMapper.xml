<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ds.goroute.mapper.MediaAssetMapper">

    <resultMap id="MediaAssetResultMap" type="com.ds.goroute.entity.MediaAsset">
        <id column="id" property="id" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="trip_id" property="tripId" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="activity_id" property="activityId" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="entity_type" property="entityType" jdbcType="VARCHAR"/>
        <result column="entity_id" property="entityId" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="media_type" property="mediaType" jdbcType="VARCHAR"/>
        <result column="url" property="url" jdbcType="VARCHAR"/>
        <result column="caption" property="caption" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="taken_at" property="takenAt" jdbcType="TIMESTAMP"/>
        <result column="latitude" property="latitude" jdbcType="NUMERIC"/>
        <result column="longitude" property="longitude" jdbcType="NUMERIC"/>
        <result column="capture_source" property="captureSource" jdbcType="VARCHAR"/>
        <result column="date_source" property="dateSource" jdbcType="VARCHAR"/>
        <result column="place_id" property="placeId" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="location_name" property="locationName" jdbcType="VARCHAR"/>
        <result column="location_source" property="locationSource" jdbcType="VARCHAR"/>
        <result column="uploaded_by" property="uploadedBy" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
        <result column="deleted_at" property="deletedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="com.ds.goroute.entity.MediaAsset">
        INSERT INTO media_assets (id, trip_id, activity_id, entity_type, entity_id, media_type, url, caption, description,
                                  taken_at, latitude, longitude, capture_source, date_source,
                                  place_id, location_name, location_source,
                                  uploaded_by, created_at, updated_at)
        VALUES (#{id,javaType=java.util.UUID,jdbcType=OTHER},
                #{tripId,javaType=java.util.UUID,jdbcType=OTHER},
                #{activityId,javaType=java.util.UUID,jdbcType=OTHER},
                #{entityType},
                #{entityId,javaType=java.util.UUID,jdbcType=OTHER},
                #{mediaType},
                #{url},
                #{caption},
                #{description},
                #{takenAt,jdbcType=TIMESTAMP},
                #{latitude,jdbcType=NUMERIC},
                #{longitude,jdbcType=NUMERIC},
                #{captureSource,jdbcType=VARCHAR},
                #{dateSource,jdbcType=VARCHAR},
                #{placeId,javaType=java.util.UUID,jdbcType=OTHER},
                #{locationName,jdbcType=VARCHAR},
                #{locationSource,jdbcType=VARCHAR},
                #{uploadedBy,javaType=java.util.UUID,jdbcType=OTHER},
                NOW(),
                NOW())
    </insert>

    <select id="selectById" parameterType="java.util.UUID" resultMap="MediaAssetResultMap">
        SELECT * FROM media_assets
        WHERE id = #{id,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
    </select>

    <select id="selectByTripId" parameterType="java.util.UUID" resultMap="MediaAssetResultMap">
        SELECT * FROM media_assets
        WHERE trip_id = #{tripId,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

    <select id="selectByActivityId" parameterType="java.util.UUID" resultMap="MediaAssetResultMap">
        SELECT * FROM media_assets
        WHERE activity_id = #{activityId,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

    <select id="countByTripId" parameterType="java.util.UUID" resultType="int">
        SELECT COUNT(*) FROM media_assets
        WHERE trip_id = #{tripId,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
    </select>

    <!-- Title and description only. The url is deliberately not updatable here:
         it is the field the legacy `memoryImageUrls` payload is built from, and
         a repair migration (V128) already had to clean it up once. -->
    <update id="updateDetails" parameterType="com.ds.goroute.entity.MediaAsset">
        UPDATE media_assets
        SET caption = #{caption},
            description = #{description},
            taken_at = #{takenAt,jdbcType=TIMESTAMP},
            date_source = #{dateSource,jdbcType=VARCHAR},
            updated_at = NOW()
        WHERE id = #{id,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
    </update>

    <!-- Expense receipts and anything else keyed by entity rather than by trip.
         Ordered the way a timeline reads: when the photo was taken, falling
         back to when it arrived for rows written before V136. -->
    <select id="selectByEntity" resultMap="MediaAssetResultMap">
        SELECT * FROM media_assets
        WHERE entity_type = #{entityType}
        AND entity_id = #{entityId,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
        ORDER BY COALESCE(taken_at, created_at) DESC
    </select>

    <select id="selectByEntityIds" resultMap="MediaAssetResultMap">
        SELECT * FROM media_assets
        WHERE entity_type = #{entityType}
        AND deleted_at IS NULL
        AND entity_id IN
        <foreach item="id" collection="entityIds" open="(" separator="," close=")">
            #{id,javaType=java.util.UUID,jdbcType=OTHER}
        </foreach>
        ORDER BY COALESCE(taken_at, created_at) DESC
    </select>

    <update id="softDeleteByEntity">
        UPDATE media_assets
        SET deleted_at = NOW(), updated_at = NOW()
        WHERE entity_type = #{entityType}
        AND entity_id = #{entityId,javaType=java.util.UUID,jdbcType=OTHER}
        AND deleted_at IS NULL
    </update>

    <update id="softDelete" parameterType="java.util.UUID">
        UPDATE media_assets
        SET deleted_at = NOW(), updated_at = NOW()
        WHERE id = #{id,javaType=java.util.UUID,jdbcType=OTHER}
    </update>
</mapper>
