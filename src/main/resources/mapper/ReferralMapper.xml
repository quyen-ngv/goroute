<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ds.goroute.mapper.ReferralMapper">
    <select id="countByInviteeId" resultType="int">SELECT COUNT(*) FROM user_referrals WHERE invitee_id = #{inviteeId}</select>
    <insert id="insert">INSERT INTO user_referrals(id, inviter_id, invitee_id, code) VALUES(#{id}, #{inviterId}, #{inviteeId}, #{code})</insert>
</mapper>
