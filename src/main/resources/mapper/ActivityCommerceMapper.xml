<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ds.goroute.mapper.ActivityCommerceMapper">
  <resultMap id="ProductMap" type="com.ds.goroute.entity.MarketplaceActivityProduct">
    <id property="id" column="id"/><result property="organizationId" column="host_organization_id"/>
    <result property="placeId" column="place_ref_id"/><result property="placeTitle" column="place_title"/>
    <result property="source" column="source"/><result property="title" column="title"/><result property="description" column="description"/>
    <result property="activityAddress" column="activity_address"/><result property="priceAmount" column="price_amount"/>
    <result property="priceCurrency" column="price_currency"/><result property="durationRaw" column="duration_raw"/>
    <result property="durationHours" column="duration_hours"/><result property="visitDurationMinutes" column="visit_duration_minutes"/>
    <result property="thumbnail" column="thumbnail"/><result property="images" column="images"/><result property="highlights" column="highlights"/>
    <result property="productStatus" column="product_status"/><result property="inventoryMode" column="inventory_mode"/>
    <result property="dataVersion" column="data_version"/><result property="createdBy" column="created_by"/><result property="updatedBy" column="updated_by"/>
    <result property="createdAt" column="created_at"/><result property="updatedAt" column="updated_at"/>
  </resultMap>
  <resultMap id="PackageMap" type="com.ds.goroute.entity.ActivityPackage">
    <id property="id" column="id"/><result property="activityBookingId" column="activity_booking_id"/>
    <result property="code" column="code"/><result property="name" column="name"/><result property="description" column="description"/>
    <result property="currency" column="currency"/><result property="basePrice" column="base_price"/>
    <result property="minQuantity" column="min_quantity"/><result property="maxQuantity" column="max_quantity"/>
    <result property="attributes" column="attributes"/><result property="cancellationPolicy" column="cancellation_policy"/>
    <result property="status" column="status"/><result property="dataVersion" column="data_version"/>
    <result property="createdBy" column="created_by"/><result property="updatedBy" column="updated_by"/>
    <result property="createdAt" column="created_at"/><result property="updatedAt" column="updated_at"/>
  </resultMap>
  <resultMap id="SlotMap" type="com.ds.goroute.entity.ActivitySlot">
    <id property="id" column="id"/><result property="packageId" column="package_id"/><result property="startsAt" column="starts_at"/>
    <result property="endsAt" column="ends_at"/><result property="timezone" column="timezone"/><result property="capacity" column="capacity"/>
    <result property="reservedQuantity" column="reserved_quantity"/><result property="soldQuantity" column="sold_quantity"/>
    <result property="blockedQuantity" column="blocked_quantity"/><result property="availableQuantity" column="available_quantity"/>
    <result property="bookingCutoffMinutes" column="booking_cutoff_minutes"/><result property="priceOverride" column="price_override"/>
    <result property="status" column="status"/><result property="dataVersion" column="data_version"/>
    <result property="updatedBy" column="updated_by"/><result property="createdAt" column="created_at"/><result property="updatedAt" column="updated_at"/>
  </resultMap>
  <resultMap id="OrderMap" type="com.ds.goroute.entity.ActivityOrder">
    <id property="id" column="id"/><result property="orderCode" column="order_code"/><result property="userId" column="user_id"/>
    <result property="organizationId" column="organization_id"/><result property="activityBookingId" column="activity_booking_id"/>
    <result property="activityTitle" column="activity_title"/><result property="slotId" column="slot_id"/>
    <result property="participantInfo" column="participant_info"/><result property="currency" column="currency"/>
    <result property="subtotalAmount" column="subtotal_amount"/><result property="taxAmount" column="tax_amount"/>
    <result property="feeAmount" column="fee_amount"/><result property="discountAmount" column="discount_amount"/>
    <result property="totalAmount" column="total_amount"/><result property="orderStatus" column="order_status"/>
    <result property="paymentStatus" column="payment_status"/><result property="voucherCode" column="voucher_code"/>
    <result property="snapshot" column="snapshot"/><result property="dataVersion" column="data_version"/>
    <result property="createdBy" column="created_by"/><result property="updatedBy" column="updated_by"/>
    <result property="createdAt" column="created_at"/><result property="updatedAt" column="updated_at"/>
  </resultMap>
  <resultMap id="OrderItemMap" type="com.ds.goroute.entity.ActivityOrderItem">
    <id property="id" column="id"/><result property="orderId" column="order_id"/><result property="packageId" column="package_id"/>
    <result property="packageName" column="package_name"/><result property="quantity" column="quantity"/>
    <result property="unitPrice" column="unit_price"/><result property="totalPrice" column="total_price"/>
    <result property="snapshot" column="snapshot"/><result property="createdAt" column="created_at"/>
  </resultMap>
  <sql id="ProductSelect">SELECT a.*,p.title AS place_title FROM activity_bookings a LEFT JOIN places p ON p.id=a.place_ref_id</sql>
  <sql id="OrderSelect">SELECT o.*,a.title AS activity_title FROM activity_orders o JOIN activity_bookings a ON a.id=o.activity_booking_id</sql>

  <insert id="insertProduct">
    INSERT INTO activity_bookings (id,source,title,description,activity_address,price_amount,price_currency,duration_raw,duration_hours,
      visit_duration_minutes,thumbnail,images,highlights,host_organization_id,place_ref_id,product_status,inventory_mode,data_version,
      created_by,updated_by,created_at,updated_at)
    VALUES (#{id},#{source},#{title},#{description},#{activityAddress},#{priceAmount},#{priceCurrency},#{durationRaw},#{durationHours},
      #{visitDurationMinutes},#{thumbnail},#{images}::jsonb,#{highlights}::jsonb,#{organizationId},#{placeId},#{productStatus},#{inventoryMode},
      #{dataVersion},#{createdBy},#{updatedBy},#{createdAt},#{updatedAt})
  </insert>
  <update id="updateProduct">
    UPDATE activity_bookings SET title=#{title},description=#{description},activity_address=#{activityAddress},price_amount=#{priceAmount},
      price_currency=#{priceCurrency},duration_raw=#{durationRaw},duration_hours=#{durationHours},visit_duration_minutes=#{visitDurationMinutes},
      thumbnail=#{thumbnail},images=#{images}::jsonb,highlights=#{highlights}::jsonb,product_status=#{productStatus},
      data_version=data_version+1,updated_by=#{updatedBy},updated_at=#{updatedAt}
    WHERE id=#{id} AND host_organization_id=#{organizationId} AND data_version=#{dataVersion}
  </update>
  <select id="findProduct" resultMap="ProductMap"><include refid="ProductSelect"/> WHERE a.id=#{id}</select>
  <select id="findProductsByOrganization" resultMap="ProductMap"><include refid="ProductSelect"/> WHERE a.host_organization_id=#{organizationId} ORDER BY a.created_at DESC</select>
  <select id="findProductsPublic" resultMap="ProductMap">
    <include refid="ProductSelect"/> JOIN host_organizations o ON o.id=a.host_organization_id
    WHERE a.host_organization_id IS NOT NULL AND a.product_status='ENABLED' AND a.inventory_mode='INTERNAL'
      AND o.operational_status='ENABLED' AND o.verification_status&lt;&gt;'SUSPENDED'
      AND (a.place_ref_id IS NULL OR p.lifecycle_status='ACTIVE')
    <if test="query != null and query != ''">
      AND (a.title ILIKE CONCAT('%',#{query},'%') OR a.activity_address ILIKE CONCAT('%',#{query},'%'))
    </if>
    ORDER BY a.created_at DESC LIMIT #{limit} OFFSET #{offset}
  </select>
  <select id="findProductsAdmin" resultMap="ProductMap">
    <include refid="ProductSelect"/> WHERE a.host_organization_id IS NOT NULL
    <if test="status != null and status != ''">AND a.product_status=#{status}</if>
    <if test="query != null and query != ''">AND a.title ILIKE CONCAT('%',#{query},'%')</if>
    ORDER BY a.created_at DESC LIMIT #{limit} OFFSET #{offset}
  </select>

  <insert id="insertPackage">INSERT INTO activity_packages (id,activity_booking_id,code,name,description,currency,base_price,min_quantity,max_quantity,attributes,cancellation_policy,status,data_version,created_by,updated_by,created_at,updated_at) VALUES (#{id},#{activityBookingId},#{code},#{name},#{description},#{currency},#{basePrice},#{minQuantity},#{maxQuantity},#{attributes}::jsonb,#{cancellationPolicy}::jsonb,#{status},#{dataVersion},#{createdBy},#{updatedBy},#{createdAt},#{updatedAt})</insert>
  <update id="updatePackage">UPDATE activity_packages SET code=#{code},name=#{name},description=#{description},currency=#{currency},base_price=#{basePrice},min_quantity=#{minQuantity},max_quantity=#{maxQuantity},attributes=#{attributes}::jsonb,cancellation_policy=#{cancellationPolicy}::jsonb,status=#{status},data_version=data_version+1,updated_by=#{updatedBy},updated_at=#{updatedAt} WHERE id=#{id} AND data_version=#{dataVersion}</update>
  <select id="findPackage" resultMap="PackageMap">SELECT * FROM activity_packages WHERE id=#{id}</select>
  <select id="findPackages" resultMap="PackageMap">SELECT * FROM activity_packages WHERE activity_booking_id=#{activityId}<if test="!includeDisabled"> AND status='ENABLED'</if> ORDER BY created_at</select>

  <insert id="insertSlot">INSERT INTO activity_slots (id,package_id,starts_at,ends_at,timezone,capacity,reserved_quantity,sold_quantity,blocked_quantity,booking_cutoff_minutes,price_override,status,data_version,updated_by,created_at,updated_at) VALUES (#{id},#{packageId},#{startsAt},#{endsAt},#{timezone},#{capacity},#{reservedQuantity},#{soldQuantity},#{blockedQuantity},#{bookingCutoffMinutes},#{priceOverride},#{status},#{dataVersion},#{updatedBy},#{createdAt},#{updatedAt})</insert>
  <update id="updateSlot">UPDATE activity_slots SET starts_at=#{startsAt},ends_at=#{endsAt},timezone=#{timezone},capacity=#{capacity},blocked_quantity=#{blockedQuantity},booking_cutoff_minutes=#{bookingCutoffMinutes},price_override=#{priceOverride},status=#{status},data_version=data_version+1,updated_by=#{updatedBy},updated_at=#{updatedAt} WHERE id=#{id} AND data_version=#{dataVersion} AND reserved_quantity+sold_quantity+#{blockedQuantity}&lt;=#{capacity}</update>
  <select id="findSlot" resultMap="SlotMap">SELECT s.*,(capacity-reserved_quantity-sold_quantity-blocked_quantity) AS available_quantity FROM activity_slots s WHERE id=#{id}</select>
  <select id="findSlots" resultMap="SlotMap">SELECT s.*,(capacity-reserved_quantity-sold_quantity-blocked_quantity) AS available_quantity FROM activity_slots s WHERE package_id=#{packageId} AND starts_at&gt;=#{from}<if test="!includeDisabled"> AND status='ENABLED'</if> ORDER BY starts_at</select>
  <update id="reserveSlot">UPDATE activity_slots SET reserved_quantity=reserved_quantity+#{quantity},data_version=data_version+1,updated_by=#{actor},updated_at=#{now} WHERE id=#{id} AND status='ENABLED' AND starts_at&gt;(#{now}+booking_cutoff_minutes*interval '1 minute') AND reserved_quantity+sold_quantity+blocked_quantity+#{quantity}&lt;=capacity</update>
  <update id="confirmSlot">UPDATE activity_slots SET reserved_quantity=reserved_quantity-#{quantity},sold_quantity=sold_quantity+#{quantity},data_version=data_version+1,updated_by=#{actor},updated_at=#{now} WHERE id=#{id} AND reserved_quantity&gt;=#{quantity}</update>
  <update id="releaseSlot">UPDATE activity_slots SET reserved_quantity=reserved_quantity-CASE WHEN #{fromReserved} THEN #{quantity} ELSE 0 END,sold_quantity=sold_quantity-CASE WHEN #{fromReserved} THEN 0 ELSE #{quantity} END,data_version=data_version+1,updated_by=#{actor},updated_at=#{now} WHERE id=#{id} AND ((#{fromReserved} AND reserved_quantity&gt;=#{quantity}) OR (NOT #{fromReserved} AND sold_quantity&gt;=#{quantity}))</update>

  <insert id="insertOrder">INSERT INTO activity_orders (id,order_code,user_id,organization_id,activity_booking_id,slot_id,participant_info,currency,subtotal_amount,tax_amount,fee_amount,discount_amount,total_amount,order_status,payment_status,voucher_code,snapshot,data_version,created_by,updated_by,created_at,updated_at) VALUES (#{id},#{orderCode},#{userId},#{organizationId},#{activityBookingId},#{slotId},#{participantInfo}::jsonb,#{currency},#{subtotalAmount},#{taxAmount},#{feeAmount},#{discountAmount},#{totalAmount},#{orderStatus},#{paymentStatus},#{voucherCode},#{snapshot}::jsonb,#{dataVersion},#{createdBy},#{updatedBy},#{createdAt},#{updatedAt})</insert>
  <insert id="insertOrderItem">INSERT INTO activity_order_items (id,order_id,package_id,quantity,unit_price,total_price,snapshot,created_at) VALUES (#{id},#{orderId},#{packageId},#{quantity},#{unitPrice},#{totalPrice},#{snapshot}::jsonb,#{createdAt})</insert>
  <select id="findOrder" resultMap="OrderMap"><include refid="OrderSelect"/> WHERE o.id=#{id}</select>
  <select id="findOrderItem" resultMap="OrderItemMap">SELECT i.*,p.name AS package_name FROM activity_order_items i JOIN activity_packages p ON p.id=i.package_id WHERE i.order_id=#{orderId}</select>
  <select id="findOrdersByUser" resultMap="OrderMap"><include refid="OrderSelect"/> WHERE o.user_id=#{userId} ORDER BY o.created_at DESC LIMIT #{limit} OFFSET #{offset}</select>
  <select id="findOrdersByOrganization" resultMap="OrderMap">
    <include refid="OrderSelect"/> WHERE o.organization_id=#{organizationId}
    <if test="status != null and status != ''">AND o.order_status=#{status}</if>
    ORDER BY o.created_at DESC LIMIT #{limit} OFFSET #{offset}
  </select>
  <select id="findOrdersAdmin" resultMap="OrderMap">
    <include refid="OrderSelect"/>
    <where>
      <if test="status != null and status != ''">AND o.order_status=#{status}</if>
      <if test="query != null and query != ''">
        AND (o.order_code ILIKE CONCAT('%',#{query},'%') OR a.title ILIKE CONCAT('%',#{query},'%'))
      </if>
    </where>
    ORDER BY o.created_at DESC LIMIT #{limit} OFFSET #{offset}
  </select>
  <update id="updateOrderStatus">UPDATE activity_orders SET order_status=#{status},data_version=data_version+1,updated_by=#{actor},updated_at=#{now} WHERE id=#{id} AND data_version=#{expectedVersion}</update>
</mapper>
