<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ds.goroute.quest.persistence.QuestEconomyMapper">

    <resultMap id="EarningMap" type="com.ds.goroute.quest.domain.QuestEarning">
        <id property="id" column="id"/>
        <result property="creatorId" column="creator_id"/>
        <result property="questId" column="quest_id"/>
        <result property="runId" column="run_id"/>
        <result property="fromUserId" column="from_user_id"/>
        <result property="source" column="source"/>
        <result property="amount" column="amount"/>
        <result property="fundingSource" column="funding_source"/>
        <result property="status" column="status"/>
        <result property="settleAt" column="settle_at"/>
        <result property="sourceReference" column="source_reference"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertEarning">
        INSERT INTO quest_earnings (
            id, creator_id, quest_id, run_id, from_user_id, source, amount, funding_source,
            status, settle_at, source_reference, created_at
        ) VALUES (
            #{id}, #{creatorId}, #{questId}, #{runId}, #{fromUserId}, #{source}, #{amount},
            #{fundingSource}, #{status}, #{settleAt}, #{sourceReference}, #{createdAt}
        )
    </insert>

    <select id="findEarningByReference" resultMap="EarningMap">
        SELECT id, creator_id, quest_id, run_id, from_user_id, source, amount, funding_source,
               status, settle_at, source_reference, created_at
        FROM quest_earnings WHERE source_reference = #{reference}
    </select>

    <insert id="insertTip">
        INSERT INTO quest_tips (
            id, quest_id, run_id, from_user_id, to_creator_id, amount, funding_source,
            message, is_message_public, created_at
        ) VALUES (
            #{id}, #{questId}, #{runId}, #{fromUserId}, #{toCreatorId}, #{amount}, #{fundingSource},
            #{message}, #{messagePublic}, #{createdAt}
        )
    </insert>

</mapper>
